var _ = require('lodash')._;

var Controller = module.exports = function(signerRegistry) {
    this.signerRegistry = signerRegistry;
};

_.extend(
    Controller.prototype,
    require('controller/internal/clientCredentials'),
    require('controller/internal/applicationPassword'),
    require('controller/internal/code')
);
