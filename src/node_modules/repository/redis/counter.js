
var CounterRepository = module.exports = function(redisClient, key) {
    this.redisClient = redisClient;
    this.key = key;
};

CounterRepository.prototype.increase = function(callback) {
    this.redisClient.incr(this.key, callback);
};